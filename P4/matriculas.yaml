# -----------------------------------------------------------------
# ¡¡ATENCIÓN!!
# Revisa que esta ruta 'path' sea la ruta ABSOLUTA
# a la carpeta raíz de tu proyecto P4.
# (La he copiado de tu error anterior, debería estar bien)
# -----------------------------------------------------------------
path: C:\Users\lucia\OneDrive\Escritorio\uniDUNIA_2\4CURSO\VC\VC\P4

# Rutas a las imágenes (relativas al 'path' de arriba)
train: TGC_RBNW/train/images
val: TGC_RBNW/val/images
test: TGC_RBNW/test/images  # Omitimos 'test' ya que no lo usamos

# --- Clases ---
# Número de clases que anotaste
nc: 1

# Nombres de las clases (¡Exactamente como en labelme!)
names: ['matricula']